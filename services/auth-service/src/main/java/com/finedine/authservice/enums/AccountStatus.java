package com.finedine.authservice.enums;

/*
 * Enum representing the different account statuses in the FineDine application.
 * Each status indicates the current state of a user's account.
 */
public enum AccountStatus {
    ACTIVE,
    INACTIVE,
    SUSPENDED
}
