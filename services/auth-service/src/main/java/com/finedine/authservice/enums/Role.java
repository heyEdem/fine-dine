package com.finedine.authservice.enums;

/**
 * Enum representing the different roles in the FineDine application.
 * Each role corresponds to one of the 4 types of users in the system.
 */
public enum Role {
    CUSTOMER,
    RESTAURANT,
    RIDER,
    ADMIN
}
