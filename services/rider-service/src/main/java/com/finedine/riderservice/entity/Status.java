package com.finedine.riderservice.entity;

/*
 * Enum representing the status of a rider in the FineDine application.
 * Each status indicates whether a rider is currently active for orders or inactive.
 */

public enum Status {
    ACTIVE,
    INACTIVE
}