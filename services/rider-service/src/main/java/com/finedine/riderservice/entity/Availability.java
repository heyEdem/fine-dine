package com.finedine.riderservice.entity;

/*
 * Enum representing the availability status of a rider in the FineDine application.
 * Each status indicates whether a rider is available for orders, unavailable, or busy.
 */
public enum Availability {
    AVAILABLE,
    UNAVAILABLE,
    BUSY
}
